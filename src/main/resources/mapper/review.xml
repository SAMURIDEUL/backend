<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.samuL.review.mapper.ReviewMapper">

    <!-- 리뷰 작성 -->
    <insert id="insertReview" parameterType="com.example.samuL.review.dto.ReviewDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reviews (place_id, user_id, rating, content, visit_date)
        VALUES (#{placeId}, #{userId}, #{rating}, #{content}, CURDATE())
    </insert>

    <!-- 리뷰 작성(사진 -->
    <insert id="insertReviewPhoto" parameterType="com.example.samuL.review.dto.ReviewPhotoDto">
        INSERT INTO review_photos (review_id, photo_url)
        VALUES (#{reviewId}, #{photoUrl})
    </insert>

    <select id="findReviewsByPlaceId" parameterType="long" resultType="com.example.samuL.review.dto.ReviewDto">
        SELECT id, place_id AS placeId, user_id AS userId, rating, content, visit_date AS visitDate, created_at
        FROM reviews
        WHERE place_id = #{placeId}
        ORDER BY created_at DESC
    </select>

    <!-- 리뷰 사진 조회 -->
    <select id="selectReviewPhotos" resultType="com.example.samuL.review.dto.ReviewPhotoDto">
        SELECT
        id,
        review_id AS reviewId,
        photo_url AS photoUrl,
        created_at AS createdAt
        FROM review_photos
        WHERE review_id = #{reviewId}
    </select>

    <!-- 리뷰 수정 시 리뷰 단독 조회 -->
    <select id="findById" resultType="com.example.samuL.review.dto.ReviewWithPhotosDto">
        SELECT
        id,
        place_id AS placeId,
        user_id AS userId,
        rating,
        content,
        visit_date AS visitDate,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM reviews
        WHERE id = #{reviewId}
    </select>

    <!-- 리뷰 내용 수정 -->
    <update id="updateReview">
        UPDATE reviews
        SET rating = #{rating},
        content = #{content},
        visit_date = #{visitDate},
        updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 리뷰 사진 삭제 -->
    <delete id="deletePhotoById">
        DELETE FROM review_photos WHERE id = #{id}
    </delete>

    <!-- 리뷰 주인 찾기(리뷰 삭제)-->
    <select id="findReviewOwner" resultType="long">
        SELECT user_id
        FROM reviews
        WHERE id = #{reviewId}
    </select>

    <!-- 리뷰 삭제 -->
    <delete id="deleteReview">
        DELETE FROM reviews
        WHERE id = #{reviewId}
    </delete>

    <!-- 리뷰 조회 -->
    <resultMap id="ReviewMap" type="com.example.samuL.review.dto.ReviewResponse">
        <id property="id" column="id"/>
        <result property="placeId" column="placeId"/>
        <result property="rating" column="rating"/>
        <result property="content" column="content"/>
        <result property="visitDate" column="visitDate"/>
        <result property="createdAt" column="createdAt"/>
        <result property="updatedAt" column="updatedAt"/>
    </resultMap>

    <select id="getUserReviews" resultMap="ReviewMap">
        SELECT
        id AS id,
        place_id AS placeId,
        rating AS rating,
        content AS content,
        visit_date AS visitDate,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM reviews
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 사진 조회 -->
    <select id="getPhotosByReviewIds" resultType="com.example.samuL.review.dto.ReviewPhotoDto">
        SELECT
        id AS id,
        review_id AS reviewId,
        photo_url AS photoUrl
        FROM review_photos
        WHERE review_id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 총 리뷰 수 -->
    <select id="countUserReviews" resultType="long">
        SELECT COUNT(*)
        FROM reviews
        WHERE user_id = #{userId}
    </select>


</mapper>