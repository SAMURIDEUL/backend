<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.samuL.place.mapper.PlaceFavoriteMapper">
    <insert id="insertFavorite">
        INSERT INTO place_favorites(user_id, place_id)
        VALUES(#{userId}, #{placeId})
    </insert>

    <delete id="deleteFavorite">
        DELETE FROM place_favorites
        WHERE user_id = #{userId}
        AND place_id = #{placeId}
    </delete>

    <select id="existsFavorite" resultType="int">
        SELECT COUNT(*)
        FROM place_favorites
        WHERE user_id = #{userId}
        AND place_id = #{placeId}
    </select>

    <select id="selectFavoritePlaceIds" resultType="long">
        SELECT place_id
        FROM place_favorites
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id = "existsUserId" parameterType="long" resultType="boolean">
        SELECT CASE WHEN EXISTS (
        SELECT 1
        FROM users
        WHERE id = #{userId}
        ) THEN TRUE ELSE FALSE END
    </select>

    <select id="existsPlaceId" parameterType="long" resultType="boolean">
        SELECT CASE WHEN EXISTS (
        SELECT 1
        FROM places
        WHERE id = #{placeId}
        ) THEN TRUE ELSE FALSE END
    </select>

<!--    <select id="existFavorite">-->
<!--        SELECT place_id-->
<!--        FROM place_favorites-->
<!--        WHERE user_id = #{userId}-->
<!--    </select>-->

</mapper>